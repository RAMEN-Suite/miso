// Deprecated: renamed to ./hierarchy. This thin re-export exists only so any stray import keeps
// working during the migration; it can be deleted once no import references it.
export { useHierarchyStore as useCollectionManagerStore } from "./hierarchy";
